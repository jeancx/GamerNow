<%= form_for([:admin, @post_comment], html: { class: 'form-horizontal post_comment' }) do |f| %>
  <% if @post_comment.errors.any? %>
    <div id="error_explanation" class="panel panel-danger">
      <div class="panel-heading">
        <h2 class="panel-title"><%= pluralize(@post_comment.errors.count, "error") %> prohibited this post_comment from being saved:</h2>
      </div>
      <div class="panel-body">
        <ul>
        <% @post_comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :user_id, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :post_id, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :post_id, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :content, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :content, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-md-offset-2 col-md-10">
      <%= f.submit nil, class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
