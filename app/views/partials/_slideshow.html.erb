<%# encoding: utf-8 %>
<div class="top-shade"></div>
<div id="full-width-slider" class="royalSlider heroSlider rsMinW">
  <% slides().each do |slide| %>
      <div class="rsContent slide4"> <a class="rsImg" href="<%= slide.image %>">#</a>
        <div class="bContainer"> <strong class="rsABlock txtCent blockHeadline"><%= slide.title %></strong></div>
      </div>
  <% end %>
</div>
<div class="bottom-shade"></div>
