<%# encoding: utf-8 %>
<header id="header">
  <%= render partial: 'partials/menu' %>
  <%= render partial: 'partials/slideshow' %>
</header>

<div class="main">
  <div class="boxed">

    <div class="page-content">

      <div class="row clearfix">
        <div class="grid_9 alpha posts">

          <div class="grid_12">

            <div class="single_post mbf clearfix">
              <h3 class="single_title">
                <i class="fa fa-file-text-o mi"></i><a href="<%= @video.link %>"><%= @video.title %></a>
              </h3>

              <div class="meta mb"> por <a href="#"><%= @video.user.name %></a> / publicado em: <%= @video.created_at %>
                /
                <a href="#comments">0 comentários</a></div>

              <iframe width="600" height="600" src="https://www.youtube.com/embed/<%= @video.vid %>" frameborder="0" allowfullscreen></iframe>

              <div class="content" style="font-size: 16px; color: #5A5A5A;">
                <% if @video.description.present? %>
                    <%= raw(@video.description.html_safe.gsub("\\r\\n", "")) %>
                <% end %>
              </div>

            </div>
            <!-- /single post -->

            <div class="share_post mbf clearfix">
              <span> Compatilhar </span>
              <ul class="socials clearfix list-inline">
                <li><%= facebook_button %></li>
                <li><%= twitter_button %></li>
                <li><%= pinterest_button %></li>
                <li><%= google_plus_button %></li>
                <li><%= tumblr_button %></li>
              </ul>
              <!-- /socials -->
            </div>
            <!-- /share -->

            <%= render partial: 'author' %>

            <div class="posts_links mbf clearfix">
              <% if @video.prev.present? %>
                  <a class="grid_6 lefter relative" href="<%= @video.prev.link %>">
                    <i class="fa fa-chevron-left"></i>
                    <small> Anterior:</small>
                    <span> <%= @video.prev.title %> </span>
                  </a><!-- /grid_6 -->
              <% end %>

              <% if @video.next.present? %>
                  <a class="grid_6 righter tar relative" href="<%= @video.next.link %>">
                    <i class="fa fa-chevron-right"></i>
                    <small> Próximo:</small>
                    <span> <%= @video.next.title %> </span>
                  </a><!-- /grid_6 -->
              <% end %>
            </div>
            <!-- /posts links -->

            <%= render partial: 'related_posts' %>

            <%= render partial: 'partials/adverts' %>

            <div class="disqus_comments" id="comments">
              <%= raw(disqus_thread) %>
            </div>

          </div>

        </div>
        <!-- end grid9 -->

        <div class="grid_3 omega sidebar sidebar_a">

          <%= render partial: 'widgets/most_waited' %>

          <%= render partial: 'widgets/polls' %>

          <%= render partial: 'widgets/last_comments' %>

          <%= render partial: 'widgets/tags' %>

        </div>
        <!-- /grid3 sidebar A -->
      </div>
      <!-- /row -->

    </div>

    <footer id="footer">
      <%= render partial: 'partials/footer_a' %>
      <%= render partial: 'partials/footer_b' %>
    </footer>

  </div>
</div>